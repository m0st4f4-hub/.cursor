---
description: "[TODO: Add description]"
globs:
  - "*.*"
alwaysApply: false
---

# ✨ Shared Core Principles & Resources

This document defines supplementary shared guidelines, resources, and definitions that apply across multiple agents but are not part of the main procedural documents like the [Execution Loop](mdc:rules-md/execution-loop.md).

## 🔗 Mandatory MCP Toolchain

This lists the core tools generally available via the MCP (Mandatory Control Plane / Platform) to most agents. Specific roles defined in [Agent Roles](mdc:rules-md/agent-roles.md) may have additional specialized tools.

| Tool                      | Purpose                            | Notes                                      |
|---------------------------|------------------------------------|--------------------------------------------|
| `mcp_taskmanager_*`       | Task tracking (Mainly Overmind)    | `mark_task_done` is non-blocking for agent |
| `edit_file`               | Apply code changes / Create file   | Adhere to [Code Edit Tag](#code-edit-tag) format. |
| `run_terminal_cmd`        | Build, test, execute scripts, Git  | Use for custom tools, env checks. **Use `| cat` for commands with pagers (e.g., `git log`). Check exit codes.** |
| `context7_*`              | Retrieve internal docs (APIs, etc.)|                                            |
| `web_search`              | Fetch external data/patterns       |                                            |
| `file_search`             | Fuzzy file path search             |                                            |
| `read_file`               | Read file contents                 |                                            |
| `list_dir`                | List directory contents            |                                            |
| `grep_search`             | Regex search within files          |                                            |
| `delete_file`             | Delete a file                      | Use with caution.                          |
| `reapply`                 | Re-attempt failed `edit_file`      |                                            |
| `run_terminal_cmd` (custom)| Execute custom scripts (`tools/*`) | e.g., `python tools/analyze_log.py`    |
*(Note: Specialized agents like Audit/UX/Styling may have additional platform-provided tools, e.g., `runPerformanceAudit`, `getSelectedElement`)*

## 🏷️ Code Edit Tag

Each `edit_file` block **MUST** include this comment header for tracking and context:

```javascript
// Task ID: <taskId> // If applicable (from MCP or similar)
// Agent Role: <agent_role> // e.g., BuilderAgent
// Request ID: <requestId> // The overall request ID
// Project: <projectName> // Optional project identifier
// Timestamp: <YYYY-MM-DDTHH:mm:ssZ> // UTC timestamp
```

## 🔧 Custom Tool Development & Usage

-   **📍 Location:** Project-specific tools should reside in the root `tools/` directory.
-   **✍️ Development:** Agents like [`BuilderAgent`](mdc:rules-md/agent-roles.md#builderagent) can create or modify tool scripts using `edit_file`.
-   **⚡ Execution:** Use `run_terminal_cmd`, specifying the interpreter and path (e.g., `python tools/run_linter.py --config project-lint.cfg`, `powershell tools/get_current_time.ps1`).
-   **📜 Tool Rules:** For discoverability and correct usage, complex tools *should* have a corresponding `.mdc` file in `rules-md/tools/` defining their purpose, arguments, I/O, and examples (e.g., `rules-md/tools/run_linter.mdc`). *(Links would use `mdc:rules-md/tools/tool-name.md`)*

## ✅ Common Custom Tools (Examples)

These are examples of Python scripts expected to be available in the `tools/` directory. Refer to their respective `.md` definitions in `rules-md/tools/` for details.

*   [`analyze_log.py`](mdc:rules-md/tools/analyze_log.md): Parses and analyzes `logs/<requestId>.json` files, often used by `ImprovementAgent` to find error patterns or inefficiencies for self-improvement.
*   [`analyze_chat_history.py`](mdc:rules-md/tools/analyze_chat_history.md): *(External Data Dependent)* Analyzes chat logs (if available and accessible) to extract user corrections and feedback, primarily for `ImprovementAgent`.
*   [`verify_environment.py`](mdc:rules-md/tools/verify_environment.mdc): Checks environment prerequisites (tool versions, dependencies).
*   [`parse_git_status.py`](mdc:rules-md/tools/parse_git_status.mdc): Provides structured output of `git status`.
*   [`check_dependencies.py`](mdc:rules-md/tools/check_dependencies.mdc): Checks for outdated or vulnerable dependencies.
*   [`run_linter.py`](mdc:rules-md/tools/run_linter.mdc): Executes configured linters/formatters.
*   [`check_versions.py`](mdc:rules-md/tools/check_versions.mdc): Checks specific tool/library versions.
*   [`consolidate_code.py`](mdc:rules-md/tools/consolidate_code.mdc): Gathers code snippets based on context.
*   [`rule_modifier.py`](mdc:rules-md/tools/rule_modifier.md): Applies structured modifications to `.md` rule files, used by `RuleWriterAgent`.
*   [`rule_validator.py`](mdc:rules-md/tools/rule_validator.md): *(Future Tool)* Validates the syntax and potentially the structure/consistency of `.md` rule files, used by `ImprovementAgent` or `RuleWriterAgent` after modifications.
*(Note: Tool availability depends on the specific project setup and the initialization procedure.)*

**Core Documents Reference:**

*   [System Prompt](mdc:rules-md/system-prompt.md): Overall framework and rules.
*   [Core Concepts](mdc:rules-md/core-concepts.md): Fundamental terminology.
*   [Execution Loop](mdc:rules-md/execution-loop.md): Detailed flow of agent operation.
*   [Log Handling](mdc:rules-md/log-handling.md): Rules for interacting with the request log.
*   [Agent Roles](mdc:rules-md/agent-roles.md): Definitions of specialized agents.
*   [Initialization Procedure](mdc:rules-md/initialization-procedure.md): Steps for setting up the ruleset. 